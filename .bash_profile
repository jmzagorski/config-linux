#!/bin/bash
#
# ~/.bash_profile
#

[[ -f ~/.wsl_profile ]] && source ~/.wsl_profile
[[ -f ~/.wslrc ]] && source ~/.wslrc
[[ -f ~/.profile ]] && source ~/.profile
[[ -f ~/.bashrc ]] && source ~/.bashrc
[[ -f ~/.bash_aliases ]] && source ~/.bash_aliases
